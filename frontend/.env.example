# Server-only Express origin. Browser requests always use same-origin /api/*
# URLs and Next.js proxies them to this origin. If omitted during local
# development, this defaults to http://localhost:5000.
BACKEND_ORIGIN=http://localhost:5000

# Production:
# BACKEND_ORIGIN=https://darkorchid-pheasant-216565.hostingersite.com
