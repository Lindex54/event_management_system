"use client";
import * as React from "react";import { useRouter } from "next/navigation";import { ShieldCheck } from "lucide-react";import { currentSession,type OrganizerSession } from "@/lib/api/organizer";import { OrganizerHeader } from "./organizer-header";import { OrganizerSidebar } from "./organizer-sidebar";
export function OrganizerLayoutShell({children}:{children:React.ReactNode}){const[session,setSession]=React.useState<OrganizerSession|null>(null),[checked,setChecked]=React.useState(false),router=useRouter();React.useEffect(()=>{const timer=window.setTimeout(()=>void currentSession().then(user=>{if(!user||!user.roles.includes("event-organizer")||!user.organizerId){router.replace("/login");return;}setSession(user);setChecked(true);}).catch(()=>router.replace("/login")),0);return()=>window.clearTimeout(timer)},[router]);if(!checked||!session)return <div className="flex min-h-svh items-center justify-center bg-background text-center"><div><span className="mx-auto flex size-11 items-center justify-center rounded-xl bg-sky-500/10 text-sky-500"><ShieldCheck/></span><p className="mt-4 text-sm font-semibold">Verifying organizer session…</p></div></div>;return <div className="min-h-screen bg-background"><OrganizerSidebar className="fixed inset-y-0 left-0 z-40 hidden w-64 border-r border-white/10 lg:flex"/><div className="min-w-0 lg:pl-64"><OrganizerHeader session={session}/><main>{children}</main></div></div>}
