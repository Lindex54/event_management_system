"use client";
import * as React from "react";import { usePathname,useRouter } from "next/navigation";import { Bell,ChevronDown,LogOut,Menu,UserRound } from "lucide-react";import { AttendeeSidebar } from "./attendee-sidebar";import { ModeToggle } from "@/components/mode-toggle";import { Avatar,AvatarFallback } from "@/components/ui/avatar";import { Button } from "@/components/ui/button";import { DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuLabel,DropdownMenuSeparator,DropdownMenuTrigger } from "@/components/ui/dropdown-menu";import { Sheet,SheetContent,SheetDescription,SheetTitle,SheetTrigger } from "@/components/ui/sheet";import { attendeeLogout,type AttendeeSession } from "@/lib/api/attendee";
const titles:Record<string,string>={"/attendee":"Dashboard","/attendee/events":"My Events","/attendee/registrations":"Registrations","/attendee/tickets":"Tickets","/attendee/notifications":"Notifications","/attendee/profile":"Profile"};
export function AttendeeHeader({session}:{session:AttendeeSession}){const[open,setOpen]=React.useState(false),pathname=usePathname(),router=useRouter();const title=titles[pathname]??"Event Details";async function logout(){await attendeeLogout();router.replace("/login");router.refresh();}const initials=session.name.split(" ").map(x=>x[0]).slice(0,2).join("").toUpperCase();return <header className="sticky top-0 z-30 flex h-17 items-center border-b border-border bg-surface/95 px-4 backdrop-blur sm:px-6"><div className="flex flex-1 items-center gap-3"><Sheet open={open} onOpenChange={setOpen}><SheetTrigger asChild><Button variant="outline" size="icon" className="lg:hidden"><Menu/></Button></SheetTrigger><SheetContent side="left" className="w-[17rem] p-0" showCloseButton={false}><SheetTitle className="sr-only">Attendee navigation</SheetTitle><SheetDescription className="sr-only">Navigate your events, registrations and tickets.</SheetDescription><AttendeeSidebar onNavigate={()=>setOpen(false)}/></SheetContent></Sheet><div><p className="font-semibold text-text-primary">{title}</p></div></div><div className="flex items-center gap-2"><Button asChild variant="outline" size="icon"><a href="/attendee/notifications"><Bell className="size-4"/></a></Button><ModeToggle/><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" className="h-10 gap-2 px-1.5"><Avatar><AvatarFallback className="bg-violet-100 font-semibold text-violet-700 dark:bg-violet-950 dark:text-violet-300">{initials}</AvatarFallback></Avatar><span className="hidden text-left md:block"><span className="block text-sm leading-4 font-semibold">{session.name}</span><span className="block text-[11px] text-text-secondary">Attendee</span></span><ChevronDown className="hidden size-3.5 md:block"/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuLabel>{session.email}</DropdownMenuLabel><DropdownMenuSeparator/><DropdownMenuItem onSelect={()=>router.push("/attendee/profile")}><UserRound/>Profile</DropdownMenuItem><DropdownMenuItem onSelect={()=>void logout()}><LogOut/>Sign out</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></header>}
